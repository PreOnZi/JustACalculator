package com.fictioncutshort.justacalculator.ui.screens

import org.jetbrains.compose.resources.DrawableResource

/**
 * The final swipe card, which addresses the player through the store they
 * installed from — so it has to name the right one.
 */
expect fun lastSwipeCard(): DrawableResource
